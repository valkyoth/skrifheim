# skrifheim Security Controls

Status: baseline control map

| Area | Control | Current Status | Evidence |
| --- | --- | --- | --- |
| Toolchain | Rust stable `1.97.1` pinned | Active | `rust-toolchain.toml` |
| Release arithmetic | Release profile keeps overflow checks enabled | Active | `Cargo.toml` |
| Core runtime | Core library crates are `no_std` | Active | `scripts/validate-engineering-policy.sh` |
| Portability baseline | Core crates avoid OS and architecture APIs, durable formats use explicit encodings and endianness, host-specific behavior stays behind explicit adapters, and architecture-specific fast paths require a portable baseline first | Active / Planned | `docs/engineering-policy.md`, `docs/VERSION_PLAN.md` |
| Extension crate boundary | Product-family primitives must live in optional extension crates when they are not mandatory database semantics, with one-way dependencies from extensions to core and independent release/pentest gates | Planned | `docs/VERSION_PLAN.md`, `docs/modularity-policy.md` |
| Extension primitive review | Every extension primitive must be classified as mandatory core, generic extension helper, or product-owned schema before implementation, with dependency direction and denial/quarantine/redaction/rebuild tests recorded | Planned | `docs/VERSION_PLAN.md`, `docs/engineering-policy.md` |
| Dependency policy | License, source, advisory, and duplicate checks | Configured | `deny.toml` |
| Release evidence | Release candidates must carry committed release notes, permanent pentest digest, SBOM, dependency-tree snapshots where relevant, current tool/crate review evidence, and gate tests before tagging | Planned | `docs/VERSION_PLAN.md`, `docs/supply-chain-security.md` |
| Release provenance | Release candidates must produce machine-readable provenance for source commit, toolchain, dependency lockfile, container image digests, SBOM, and gate output, with reproducibility claims explicit per profile | Planned | `docs/VERSION_PLAN.md`, `docs/supply-chain-security.md` |
| External source lock | Legal, compliance, cryptographic, storage-format, parser, and conformance claims must pin exact external sources, hashes, dates, licenses, and non-claims before implementation evidence is accepted | Planned | `docs/VERSION_PLAN.md`, `docs/supply-chain-security.md` |
| Security reporting | Private-first vulnerability handling | Configured | `SECURITY.md` |
| Unsafe code | Forbidden in scaffold | Active | `scripts/validate-security-policy.sh` |
| Modularity | Focused crates and file-size gate | Active | `docs/modularity-policy.md` |
| Canonical truth | Facts are versioned and evidence-bound | Scaffolded | `skrifheim-fact` |
| Canonical fact signatures | Durable ingest must verify a domain-separated canonical fact transcript bound to tenant, world revision, schema root, policy epoch, actor authority, payload digest, evidence, labels, and key lifecycle state | Planned | `docs/VERSION_PLAN.md` |
| Fact identity allocation | Fact ID generation strategy must be selected before transaction write sets and MVCC harden; IDs must not become authorization capabilities or sole tenant/storage isolation keys | Planned | `docs/VERSION_PLAN.md` |
| Transaction read-your-writes | In-memory transaction state must expose a transaction's own uncommitted writes to its reads while hiding them from other transactions until durable commit | Planned | `docs/VERSION_PLAN.md` |
| MVCC and commit protocol | Durable writes must use engine-assigned commit sequences, optimistic serializable validation, ordered WAL commit roots, group commit, and publish versions only after the durable barrier succeeds | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Counter exhaustion | LSNs, WAL generations, transaction IDs, commit sequences, file numbers, manifest generations, crypto epochs, lifecycle sequences, anchor generations, and backup generations require checked arithmetic, pre-maximum exhaustion thresholds, rollover rules where safe, and fail-closed behavior where identity cannot be preserved | Planned | `docs/VERSION_PLAN.md` |
| World revisions | Stable `WorldId` identifies a branch, while immutable `WorldRevisionId` identifies content/root state; world-head updates require compare-and-swap and promotion uses three-way merge over fork base, current parent head, and candidate head | Planned | `docs/VERSION_PLAN.md` |
| World promotion safety | Promotion and rollback preflight expose deterministic conflict categories before merge/promotion logic exists, using sorted-slice scans to avoid transient set allocation | Scaffolded | `skrifheim-world` |
| Local rollback retention | Snapshot roots must be signed, policy-bound facts that restore only through read-only inspection, recovery/archive worlds, simulation, or policy-checked production promotion; compaction must preserve protected rollback roots unless an explicit authorized purge or crypto-erasure proof exists | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Point-in-time recovery and change streams | PITR, change streams, and deterministic replay tracing must bind to committed manifest generations, WAL commit roots, world revisions, policy/schema/audit roots, and redacted operation categories | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Classification | Labels and clearance checks | Scaffolded | `skrifheim-policy` |
| External error shape | `SkrifheimError::public_message()` provides generic messages for trust-boundary responses; `Display` is internal diagnostic output only | Scaffolded | `skrifheim-core` |
| Diagnostic redaction | Sensitive fact payloads, fact actor attribution, labels, policy tokens, signatures, authority contexts, sovereignty scopes, query-result inputs, result classifications, planner decisions, policy proofs, query requests, query plans, query intent metadata, planner decision state, storage headers, world metadata, audit metadata, and exact query/proof input counts use redacted `Debug`; direct sensitive `Debug` derives are release-gate blocked | Scaffolded | `scripts/validate-security-policy.sh` |
| Sensitive equality | Security labels, policy-token sets, policy-token slots, sovereignty scopes, signature envelopes, signature sets, planner decisions, policy proofs, query-result inputs, result classifications, query requests, query plans, and audit metadata do not expose derived or manual `PartialEq`/`Eq`; structural comparison is explicit and not for policy, audit, or crypto decisions | Scaffolded | `scripts/validate-security-policy.sh` |
| Signature-set bounds | Maximum signature-envelope count and bounded signature key identifiers before durable ingest accepts untrusted commits | Scaffolded | `skrifheim-crypto` |
| Signature algorithm admission | Named and hybrid signature algorithm identifiers require a closed approved list | Scaffolded | `skrifheim-crypto` |
| Threshold signatures and quorum proofs | Multi-party authority must use an explicit pre-1.0 threshold-signature or bounded quorum multi-signature proof model before manifests, law-pack admission, key ceremonies, declassification, break-glass grants, backup restore, or release operations depend on it | Planned | `docs/VERSION_PLAN.md` |
| Fact-builder complexity | O(n log n) deduplication, fail-fast builder bounds, and bounded text/byte fact payloads | Scaffolded | `skrifheim-fact` |
| Blast-radius invalidation | Forward causal DAG traversal identifies tainted downstream facts, projections, releases, and AI artifacts | Planned | `docs/IMPLEMENTATION_PLAN.md` |
| Declassification | Write-downs require signed provenance-bearing declassification proofs | Planned | `docs/IMPLEMENTATION_PLAN.md` |
| AI derivation cones | AI writes are capability-scoped with classification ceilings and traceable derivation cones | Planned | `docs/IMPLEMENTATION_PLAN.md` |
| Confidence and mandatory access control | Propagated confidence is evaluated together with mandatory access control; the math is reserved for `v0.23.1` before query execution implements caused-by confidence propagation | Planned | `docs/IMPLEMENTATION_PLAN.md`, `docs/VERSION_PLAN.md` |
| Collaborative text convergence | Optional publishing, forum, messenger, collaboration, and local-first extensions must choose OT, an established CRDT, or a documented custom model before release or merge semantics rely on collaborative text; the first concrete decision is reserved for `v1.1.1` | Planned | `docs/VERSION_PLAN.md`, `docs/publishing-extension-target.md` |
| Secure bootstrap and public-origin binding | First-run bootstrap must use one-time token proofs, setup fingerprints, origin binding, trusted-proxy validation, secret-free audit events, and automatic lockout after completion | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Public descriptor and scheduled operation policy | Public aliases, descriptors, scheduled publishing, cache purge/warm actions, and read-only config export must be audited, origin-aware, private where required, and unable to expose secrets or protected responses | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Relationship graph and timeline policy | Optional relationship/feed extensions need viewer-context visibility for relationship denials, audience limits, scoped roles, search, timelines, counters, and notification projections; implementation starts at `v1.5.0` | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Media metadata authorization | Encrypted media references, normalized variants, captions, takedowns, processing state, and short-lived delivery grants must re-check content visibility and never expose raw object keys or protected metadata; implementation starts at `v1.5.1` | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Moderation, safety, consent, and transparency | Moderation labels, appeals, statements of reasons, stackable safety labels, consent ledgers, ranking explanations, and transparency records are policy inputs for optional extensions; implementation starts at `v1.5.3` | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Hierarchical discussion and scoped permissions | Nested discussion spaces, long-lived threads, read-state, watch subscriptions, sanitized-content provenance, scoped capabilities, trust hooks, and moderation workflows must be policy-bound and rebuildable; implementation starts at `v1.3.0` | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Source-state object and bundle admission | Forge-style applications need typed application object domains, immutable source-state records, proof-carrying bundle manifests, remote accept/deny/quarantine/more-evidence decisions, and stale-alias rollback protection; implementation starts at `v1.4.0` | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Resource-budgeted verification | Large source-state worlds and bundles must verify through bounded-batch, lazy-cone, or full-world modes with explicit memory, graph, body-size, and parallelism budgets before trust or materialization; implementation starts at `v1.4.1` | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Operation, event, explanation, and context records | Extension workflows need append-only operation records, non-authoritative bounded events, deterministic event-to-fact compilation, auditable explanations, and bounded redacted context packs; implementation starts in post-1.0 extension tracks | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Extension and import boundaries | Plugins, themes, extension hooks, and legacy imports require manifest validation, host-grant proofs, capability simulation, dry-run import reports, permission-gap reporting, quarantine, and idempotent checkpoints before trusted writes | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Timing side channels | Policy-token checks use fixed-slot byte sets, admitted `subtle` constant-time equality, bounded scans, defensive fixed-slot copying, and fail-closed oversize guards; token-set union canonicalization remains structural and non-authorization only, and production statistical timing evidence remains required | Scaffolded | `docs/engineering-policy.md` |
| Policy-token hot path | Canonical policy strings must remain at catalog boundaries while authorization hot paths move to compact catalog IDs or keyed fixed-width tags with constant-shape operations and timing evidence | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Query inference | Public query plans expose aggregate proof/decision state, not per-label decisions; query requests do not expose raw result-input slices after construction, query-result inputs do not expose raw metadata accessors publicly, and pre-execution aggregation requires inference budgets, cohort controls, query-history-aware differencing detection, and purpose-bound audit | Scaffolded / Planned | `skrifheim-query`, `skrifheim-policy`, `docs/VERSION_PLAN.md` |
| Access-pattern leakage | Encrypted storage and redacted results still leak touched roots, indexes, timing, result size, cache behavior, and query shape unless a deployment chooses padding, batching, private-query, delayed-response, or offline workflows; high-assurance profiles must reject unsupported leakage shapes | Planned | `docs/VERSION_PLAN.md`, `docs/threat-model.md` |
| Query request memory | Query requests and aggregate policy helpers fail closed for empty or oversized input sets, invalid aggregate proofs mask exact input counts to a public zero sentinel, use constructor-enforced input limits, store one exact-capacity result-input vector, and keep an explicit fixed result-input memory budget | Scaffolded | `skrifheim-query`, `skrifheim-policy` |
| Simulation isolation | Simulation queries must choose writable temporary world forks or read-only counterfactual overlays before native query AST work; source worlds must not be mutated by simulation without an explicit future authorization path | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Trusted planner context | Public query requests must not supply stored-data labels or result-classification inputs; bound plans must tie tenant, identity, attestation evidence, world revision, manifest root, policy epoch, query digest, purpose, expiry, and nonce to the proof and revalidate those bindings at execution | Planned | `docs/VERSION_PLAN.md` |
| Query-result classification | Allowed query plans propagate output classification, compartments, releasability/dissemination, exact or saturated sovereignty scope, policy epoch proof state, PII-derived output, AI-processing eligibility, and confidence-threshold policy metadata; non-allow plans expose only public sentinel metadata | Scaffolded / Planned | `skrifheim-policy`, `skrifheim-query`, `docs/VERSION_PLAN.md` |
| Query proof construction | Planner decisions and policy proofs are constructed inside `skrifheim-policy`; proof construction masks non-allow result metadata, and public callers inspect aggregate state but cannot forge non-allow proofs with sensitive result metadata | Scaffolded | `skrifheim-policy`, `scripts/validate-security-policy.sh` |
| Crypto agility | Algorithm and signature envelopes | Scaffolded | `skrifheim-crypto` |
| Key hierarchy | Root trust, deployment, region, tenant, compartment, segment, and data key parent metadata with tenant deployment/region and segment/data compartment binding | Scaffolded | `skrifheim-crypto` |
| Scoped key release | Production profiles must release keys through a scoped KMS/HSM/privilege-separated key provider; the main database process must not hold root, deployment, or unrestricted tenant keys | Planned | `docs/VERSION_PLAN.md`, `docs/encryption-architecture.md` |
| Key lifecycle | Creation, activation, rotation preflight, strict epoch progression for key-material changes, separate lifecycle event ordering for metadata-only compromise/quarantine/destruction/erasure states, unsafe-parent rejection, destruction, destruction-gated crypto-erasure metadata, and planned key-subtree revocation before real key-use paths | Scaffolded | `skrifheim-crypto`, `docs/encryption-architecture.md` |
| Encryption domains | Tenant, region, classification, compartment, world, WAL, segment, projection, backup, export, AI artifact, WASM/plugin secret, and audit-log blast-radius boundaries with exact merge compatibility checks | Scaffolded | `skrifheim-crypto` |
| Production AEAD and digest engine | WAL and segment bodies must move from structural CRC/unkeyed digest checks to admitted SHA-3/SHAKE digests plus AEAD authentication, WAL anti-splicing identity, encrypted inner storage metadata, separated digest semantics, and crypto-erasure granularity before manifests or recovery claim tamper resistance | Planned | `docs/VERSION_PLAN.md`, `docs/encryption-architecture.md` |
| Entropy and nonce generation | Production crypto paths must use an admitted entropy/CSPRNG provider, fail closed when entropy is unavailable, exclude deterministic test RNGs from production profiles, and prove nonce uniqueness under the storage crash/restart model | Planned | `docs/VERSION_PLAN.md`, `docs/encryption-architecture.md` |
| Index and projection encryption | Secondary, graph, search, vector, columnar, cache, and compaction projection surfaces must use classified projection encryption domains, require encryption at rest, disallow plaintext temporary files, reject incompatible domain mixing, and use redacted diagnostics with explicit structural comparison | Scaffolded | `skrifheim-crypto` |
| Sealed private realms and blind remotes | Encrypted source-state hosting must support private keyed plaintext IDs, public ciphertext storage IDs, protected metadata policy, recipient slots, leak-scan result facts, and trusted/blind/split-trust remote modes | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Memory secrecy | Secret material enters crypto APIs through bounded non-clone redacted `SecretBytes` wrappers backed by admitted `sanitization` clear-on-drop storage | Scaffolded | `docs/memory-secrecy.md` |
| Identity and audit events | Security-relevant actions require typed actor attribution, require trusted-clock audit-event construction with bounded lookback, reject stale/future attestation evidence, require expiring attested device/workload context for break-glass events, and bind event metadata to signed audit-log encryption domains | Scaffolded | `skrifheim-audit` |
| Chained audit roots | Audit records must be sequence-chained, included in manifests, anchored through freshness roots, and mandatory-audit operations must fail closed when durable audit emission fails | Planned | `docs/VERSION_PLAN.md`, `docs/encryption-architecture.md` |
| Atomic audit and commit | Durable commits for mandatory-audit operations must authenticate an audit-record digest in the commit transcript or commit a transactional audit outbox in the same WAL transaction, with recovery preventing duplicate emission or success without audit evidence | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Break-glass access | Break-glass audit metadata does not bypass policy by itself; scoped emergency access semantics, approval requirements, legal/audit isolation, identity-proofed one-time capability options, AI-assisted evidence limits, and denial behavior are reserved for `v0.26.1` before policy-aware query planning | Planned | `docs/VERSION_PLAN.md`, `docs/threat-model.md` |
| Encryption control plane | Key hierarchy, lifecycle, domains, memory secrecy, encrypted projections, and compromise handling | Planned | `docs/encryption-architecture.md` |
| Legal/compliance planning | Instance/node, data, and operation passports with signed law-pack metadata and legal operation/transfer decisions | Planned | `docs/hyve-cluster-and-compliance-roadmap.md` |
| Platform identity and product boundaries | Identity authority, shared account facts, product-owned data, operator/support/service identities, guardian consent, derived claims, and service secret/key boundaries must stay separated by policy, tenant, and encryption domain | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Privacy rights and message boundaries | Export, delete, deactivation, retention, legal hold, breach evidence, E2EE DM metadata, server-blind collaboration envelopes, cross-product orchestration, and searchable encrypted mailbox access must preserve privacy rights without creating plaintext indexing or unrestricted server-side decrypt paths | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Sovereign placement | Placement and failover decisions constrained by jurisdiction, compliance, data category, and legal basis | Planned | `docs/hyve-cluster-and-compliance-roadmap.md` |
| Cluster control plane | Future Hyve cells, control plane, policy-scoped tunnels, witness nodes, and compliance autopilot | Planned | `docs/hyve-cluster-and-compliance-roadmap.md` |
| Freshness anchoring | Production profiles require a non-rollbackable freshness anchor such as TPM/HSM monotonic state, remote witness/transparency service, WORM/offline operator checkpoint, or threshold-held external checkpoint; active startup fails closed on stale local roots | Planned | `docs/VERSION_PLAN.md`, `docs/threat-model.md` |
| Freshness anchor provider | At least one host-boundary reference provider must implement idempotent compare-and-advance, authenticated/versioned transport, secure credential storage, unavailable-provider fail-closed behavior, provisioning/replacement, witness equivocation detection, and disaster-recovery rules before production startup depends on anchors | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Tamper evidence | Current WAL/segment checks detect structural corruption only: CRC64 and unkeyed content digests are recomputable by a local disk writer. Production tamper resistance requires AEAD authentication, WAL anti-splicing, signed manifests, chained audit roots, freshness anchoring, or another keyed integrity root before stored bytes are trusted against write-capable attackers | Planned | `docs/encryption-architecture.md`, `skrifheim-storage`, `docs/VERSION_PLAN.md` |
| Crypto primitive sequencing | `v0.18.3` admits production digest/AEAD primitives and generic envelopes only; concrete WAL, block, segment, and manifest formats instantiate those primitives after their byte layouts are frozen, with no production storage-encryption claim before that | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Durable format compatibility | Every durable format milestone must commit golden compatibility fixtures when the format is introduced, including AEAD envelopes, WAL v2, block tables, compression metadata, range tombstones, and manifests | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Format version compatibility | WAL, block/table, segment, and manifest formats must define major/minor semantics, required/ignorable feature bits, minimum reader/writer versions, canonical encoding, unknown-field behavior, and forward/backward read policy before migrations automate upgrades | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Storage single-writer and migration safety | Manifest roots require a storage-directory identity, single-writer lease, stale-lease recovery bound to freshness/trusted time, and downgrade-protected idempotent format migrations before production hardening | Planned | `docs/VERSION_PLAN.md` |
| Manifest authentication | Authoritative manifests require keyed authentication and encrypted inner metadata before contents are trusted; asymmetric signatures, quorum proofs, and non-repudiation remain separate later controls | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Manifest key bootstrap and rotation | Manifest outer headers are key-location hints only; database identity and manifest generation determine key-provider scope, arbitrary provider/key scopes from disk are rejected, candidate lookup attempts are bounded, errors are uniform, lookup timeouts/rate limits apply, dual-key rotation handles crash recovery, and lost or compromised manifest keys fail closed except for explicit historical/recovery modes | Planned | `docs/VERSION_PLAN.md` |
| Manifest authority and cleanup safety | The manifest is the sole authority for live tables, world heads, schemas, key state, WAL checkpoint, audit root, freshness anchor, and protected roots; cleanup, migration, writers, checkpointing, manifest changes, and obsolete-file deletion must be serialized by the storage-directory lease | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Storage parser fuzzing | WAL and segment byte parsers get a dedicated early fuzz baseline before the broader fuzz/property milestone | Planned | `docs/VERSION_PLAN.md` |
| Physical storage layout | Production storage requires a block-structured layout with canonical key ordering, aligned encrypted blocks, restart/offset metadata, sparse indexes, filters, compression IDs, checksums, AEAD envelopes, and policy-domain partitioning before manifests harden | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Physical block authentication | Encrypted blocks must authenticate database ID, storage generation, table/segment identity, block ordinal or offset, block kind, format/feature versions, tenant, encryption domain, policy epoch, crypto epoch, compression format, and original length to reject cross-file, cross-offset, stale, duplicate, and wrong-kind replay | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| WAL v2 and torn-tail recovery | WAL v2 must authenticate ordering metadata, bind commit roots, reject splicing, validate existing tails before append, and repair recoverable torn tails without accepting interior corruption | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| WAL v1 stopgap hardening | Until WAL v2 is active, scaffold writers must poison themselves after append/sync errors, validate existing tails before append, expose durable or ambiguous status, and support idempotent transaction retry semantics | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Storage failure injection | Storage root transitions require deterministic failpoints, subprocess crash tests, torn-sector/corrupted-block matrices, and reference-model recovery checks before production trust | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Filesystem durability matrix | Local filesystem support requires proven locking, rename, fsync, directory-sync, and power-cut behavior; network filesystems remain unsupported until those semantics are proven | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Storage kernel and compaction | The storage kernel must provide WAL-backed memtables, immutable sorted runs, version sets, streaming and merge iterators, filters, caches, minimal compaction, protected-root accounting, and online scrubbing before durable query paths depend on it | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Early background work scheduler | Flush, compaction, scrub, deletion, and checkpoint-adjacent cleanup need bounded concurrency, foreground protection, audit/recovery priority, cancellation, tenant fairness, and starvation metrics before the full runtime worker-pool model arrives | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Range tombstones | Range deletions require encoded, fragmented, coalesced, snapshot-visible, compaction-safe semantics before the table format freezes, including interaction with legal deletion and protected rollback roots | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Space reclamation and resource governance | WAL pruning, obsolete files, tombstone retention, oldest-active-snapshot watermarks, explicit snapshot leases, per-tenant pin quotas, rollback/archive/legal-hold roots, orphan recovery, file-number no-reuse, preallocation, free-space margins, disk-exhaustion escape reserve, compaction debt, and per-tenant I/O admission must prevent unsafe deletion and storage exhaustion | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Graceful shutdown and restart storms | Shutdown must stop write admission, drain/cancel transactions, resolve group and ambiguous commits, stop background publication, order audit and anchor updates, release leases only after safe durability, bound forced termination, and protect against repeated startup-failure loops | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Storage spine | A complete `WriteBatch -> WAL v2 -> durable barrier -> memtable -> table flush -> manifest swap -> restart recovery -> point read -> compaction` path must pass before later database features depend on storage | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Host fast-path isolation | `io_uring`, direct-I/O, mmap, SIMD, aligned allocation, and platform syscall optimizations must remain optional host-boundary crates with portable fallbacks and Miri plus sanitizer or equivalent verification where applicable | Planned | `docs/VERSION_PLAN.md`, `docs/engineering-policy.md` |
| Hot/cold tiering and blob deduplication | Tier movement and deduplication must stay within compatible tenant, compartment, policy, key epoch, legal, snapshot, rollback, backup, and audit boundaries and must not leak plaintext equality across domains | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Durable indexes | Primary fact, causal, supersession, invalidation, hidden fact, tombstone, and snapshot visibility indexes must be durable, rebuildable, and verified against canonical fact blocks | Planned | `docs/VERSION_PLAN.md` |
| Concurrency model testing | Commit coordination, world-head CAS, checkpoint/WAL rotation, compaction publication, cache pinning, and key-rotation races require model checking or equivalent generated history validation | Planned | `docs/VERSION_PLAN.md` |
| Early performance and integration evidence | Storage/recovery, transaction durability, fixed-slot policy checks, mirrored-footer overhead, authenticated request-context integration, benchmark methodology, and supported rootless container volume profiles get smoke evidence before query/API design freezes | Planned | `docs/VERSION_PLAN.md` |
| Database SLO evidence | WAL bytes/fsyncs per commit, commit latency, p99.9 tail latency, read/write/space amplification, recovery rate, startup time, compaction debt, stall time, cache hit rate, mixed workload interference, endurance behavior, and memory budgets must be measured before production claims | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Redacted operational observability | Health and metrics must expose anchor availability, WAL/checkpoint lag, compaction debt, scrub coverage, quarantines, backup health, restore-drill age, key-provider latency, resource saturation, quota denials, and legal-policy denial categories without leaking tenant, compartment, fact, key, world, actor, policy-token, or query values | Planned | `docs/VERSION_PLAN.md` |
| Segment encoding overhead | `v0.18.0` keeps full-width mirrored segment footers to preserve header/body/footer mismatch and trailing-byte detection; `v0.18.2` rejects legacy kind-unbound footers on normal reads so v2 kind binding cannot be downgraded; compact trailers may be reconsidered only after segment sizing and integrity roots are stronger | Scaffolded | `docs/VERSION_PLAN.md`, `skrifheim-storage` |
| Segment size bounds and publication | Segment headers reject empty and oversized bodies before future segment readers allocate; host segment writers and readers enforce the same in-memory body cap so accepted writes are readable by the same host library; staged publication distinguishes target durability from cleanup-pending state, redacts storage paths in diagnostics, reserves the staging filename namespace from published targets, and provides strict owned staging-file cleanup that preserves lookalike published names and rejects symlink cleanup directories | Scaffolded | `skrifheim-storage`, `skrifheim-storage-host` |
| WAL frame format | Fixed-size append-only WAL frame headers validate magic, version, record kind, reserved bytes, encrypted-body length, non-zero body CRC presence, non-zero crypto epoch, encryption key, WAL encryption-domain tenant binding, and caller-provided expected domain matching before file I/O exists | Scaffolded | `skrifheim-storage` |
| WAL host-file I/O | Host-only WAL append/read helpers live outside the `no_std` core crates, append encrypted frame bodies, create and reopen Unix WAL files owner-only, fsync parent directories on every successful writer open, reject Unix symlink WAL paths on write and read, fail closed on unsupported non-Unix targets until hardened platform controls exist, verify CRC64-ECMA over encrypted frame bodies, read sequentially, reject unexpected domains, and detect partial headers or bodies | Scaffolded | `skrifheim-storage-host` |
| World identity collision resistance | Deterministic world IDs are length-separated, tenant-scoped, non-secret BLAKE3-derived compact handles for the scaffold; BLAKE3 is not used as a signature or encryption primitive, and production storage authority must move to admitted SHA-3/SHAKE full-width world identity digests before compact IDs become storage keys | Scaffolded | `skrifheim-world` |
| Quantum-aware digest policy | Configurable digest strength profiles for SHA3-256, SHA3-384, SHA3-512, SHAKE256-256, and SHAKE256-512 with full-width world, content, and manifest digest types, explicit non-authentication structural comparison, and `subtle`-backed digest equality helpers before durable storage authority | Scaffolded | `skrifheim-crypto` |
| Tenant world quotas | Tenant-level world count and aggregate tracked fact-reference budgets are required before storage-backed world creation is exposed | Planned | `docs/IMPLEMENTATION_PLAN.md` |
| AI authority | AI artifacts are derived, not truth | Planned | `docs/IMPLEMENTATION_PLAN.md` |
| Vector search projection | Vector search remains a policy-partitioned rebuildable projection with manifest-bound watermarks, model/provenance metadata, deletion visibility, stale-policy rejection, and recall/leakage tests | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Publishing extension isolation | Public/private world split for optional publishing-style extension crates | Planned | `docs/publishing-extension-target.md` |
| Rootless container | Podman smoke path | Scaffolded | `scripts/podman_smoke.sh` |
| Approval roles | Break-glass, law-pack admission, key ceremonies, declassification, backup restore, and release operations need executable local roles and single-maintainer fallback policy before threshold-approval language becomes enforceable | Planned | `docs/VERSION_PLAN.md` |
| API credential lifecycle | API credentials and sessions must bind tenant, principal, device, workload, purpose, policy epoch, scopes, expiry, nonce, and revocation epoch, with revocation roots tied into durable state where required | Planned | `docs/VERSION_PLAN.md`, `docs/threat-model.md` |
| Policy evaluator safety | Law-pack and policy-pack evaluation must be deterministic, resource-bounded, side-effect-free, and unable to use network, filesystem, random, wall-clock, or process state during access decisions | Planned | `docs/VERSION_PLAN.md`, `docs/threat-model.md` |
| Host runtime isolation | Tenant and classification-aware resource pools must protect audit, recovery, freshness-anchor, query, projection, compaction, backup, export, legal, and AI jobs from noisy-neighbor starvation; microarchitectural isolation is a non-claim unless a stronger deployment profile provides it | Planned | `docs/VERSION_PLAN.md` |
| Production backup and restore | Backup/restore requires online consistency, bounded incremental chains, synthetic-full consolidation, full and incremental recovery-point semantics, resumable verified chunks, backup key rotation, retention/orphan cleanup, new-identity versus in-place restore modes, restore drills, corruption injection, final schema/retention/quota/resource/observability/legal qualification, and measured RPO/RTO before production claims | Planned | `docs/VERSION_PLAN.md` |
| Backup sovereign placement | Backup destination jurisdiction, storage-provider jurisdiction, temporary upload and restore staging, cross-region restore, synthetic-full generation, policy/key/law/passport placement changes, and deletion or quarantine of misplaced fragments must be qualified before production backup claims | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |
| Final release qualification | The release candidate must rerun endurance, crash/recovery, corrupted-backup, upgrade, downgrade-rejection, and mixed legal/placement/backup/foreground workload evidence after late-stage integrations, comparing against stored `v0.50.0` budgets | Planned | `docs/VERSION_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md` |

## Admission Rule

Security-sensitive features do not graduate from planned to active until they have tests, documentation, failure-mode analysis, and release-gate coverage.
