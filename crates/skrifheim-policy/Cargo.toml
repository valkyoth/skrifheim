[package]
name = "skrifheim-policy"
description = "Classification-aware policy planning primitives for skrifheim."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
skrifheim-core = { version = "0.7.0", path = "../skrifheim-core" }

[lints]
workspace = true
