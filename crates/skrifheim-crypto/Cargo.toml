[package]
name = "skrifheim-crypto"
description = "Crypto-agile envelope types for skrifheim."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
skrifheim-core = { version = "0.3.0", path = "../skrifheim-core" }

[lints]
workspace = true
