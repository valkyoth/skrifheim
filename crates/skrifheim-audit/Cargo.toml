[package]
name = "skrifheim-audit"
description = "Identity and audit-event model for skrifheim."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
skrifheim-core = { version = "0.15.0", path = "../skrifheim-core" }
skrifheim-crypto = { version = "0.15.0", path = "../skrifheim-crypto" }

[lints]
workspace = true
