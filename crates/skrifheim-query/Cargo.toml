[package]
name = "skrifheim-query"
description = "Policy-aware query planning primitives for skrifheim."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
skrifheim-core = { version = "0.6.0", path = "../skrifheim-core" }
skrifheim-policy = { version = "0.6.0", path = "../skrifheim-policy" }

[lints]
workspace = true
