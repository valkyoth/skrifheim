# Contributing

Run `scripts/checks.sh` before proposing changes.

Keep changes small, crate boundaries clear, and tests close to the behavior being changed. Security-sensitive changes must include documentation of the invariant being protected and at least one negative test.
